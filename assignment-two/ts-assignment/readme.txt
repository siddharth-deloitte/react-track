refer to the problem statement in problem.txt file

To run the function use the 
- `tsc ` command with the file name as argument. 
- After generaing the .js file run the file using the `node `file name to see the output on the terminal.

or
- run the `ts-node ` filename command to get the output on the terminal